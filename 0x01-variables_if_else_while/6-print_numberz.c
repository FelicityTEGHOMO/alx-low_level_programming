#include <stdio.h>

/**
 * main - print all digit numbers of base 10.
 *
 * Return: 0 for success
 */
int main(void)
{
	int i;

	for (i = 48; i <= 48 + 9; i++)
	{
		putchar(i);
	}
	putchar('\n');

	return (0);
}
